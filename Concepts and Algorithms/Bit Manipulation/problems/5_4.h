#pragma once

/** 5.4 Explain what the following code does: ((n & (n-1)) == 0).
 * =============================================================
 * if n = abcde1000
 * then n-1 = abcde0111
 *
 * Answer: ((n & (n-1)) == 0) checks if n is a power of 2 (or 0).
**/
